<view class="wrapped">
      <view class="con">
        <view class="top">
              <button class="accept">已接贴</button>
        </view>
        <block wx:for="{{addBox}}" wx:key="index" class="box">
              <view class="middle">
                    <view class="content">内容:</view>
                    <view class="content">{{content[index]}}</view>
                    <view class="content">创建时间:</view>
                    <view class="content">{{create[index]}}</view>
              </view>
        </block>
      </view>
</view>